## § 5 — Backend Supabase

### Client Supabase
- Fichier(s) d'init (sortie commande A) :
  - `lib/supabase.ts`
- Méthodes d'auth utilisées (sortie commande F) :
  - `supabase.auth.getSession`
  - `supabase.auth.getUser`
  - `supabase.auth.onAuthStateChange`
  - `supabase.auth.resend`
  - `supabase.auth.resetPasswordForEmail`
  - `supabase.auth.signInWithPassword`
  - `supabase.auth.signOut`
  - `supabase.auth.signUp`
  - `supabase.auth.updateUser`

### Tables référencées
Liste unique des tables trouvées (sortie commande B, juste les noms) :

(aucun trouvé)

### RPC appelées
Liste unique des fonctions RPC (sortie commande C, juste les noms) :

(aucun trouvé)

### Storage buckets
Liste unique des buckets (sortie commande D, juste les noms) :

(aucun trouvé)

### Realtime channels
Liste unique des channels (sortie commande E, juste les noms) :

(aucun trouvé)
